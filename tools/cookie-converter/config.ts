import { Cookie } from "lucide-react";
import type { ToolConfig } from "@/lib/tools-meta";

export const config: ToolConfig = {
  slug: "cookie-converter",
  name: "Cookie Converter",
  category: "Converters",
  description:
    "Convert cookies between Netscape, JSON and Header string formats.",
  icon: Cookie,
};
